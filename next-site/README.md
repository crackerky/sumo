# Next-site (Next.js + Tailwind + Framer Motion)

このフォルダは既存サイトを崩さずに試作するための Next.js スケルトンです。

セットアップ:

```bash
cd next-site
npm install
npm run dev
```

開発は `http://localhost:3000` を確認してください。

既存の `assets/` を `next-site/public/assets/` にコピーすれば、画像等を流用できます。
